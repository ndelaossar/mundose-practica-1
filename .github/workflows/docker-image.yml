name: Docker Image CI

on:
  push:
    branches: [ "main" ]

env:
  REGISTRY: pinoched

jobs:
  build-docker:
    runs-on: ubuntu-latest
    steps:
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
    
    - uses: actions/checkout@v3
    
    - name: Build the Docker Image Frontend
      run: |
        VERSION=$(git rev-parse --short "$GITHUB_SHA")
        docker build . --file ./src/app/Dockerfile --tag $REGISTRY/frontend:$VERSION
        docker push $REGISTRY/frontend:$VERSION

    - name: Build the Docker Image Consumer
      run: |
        VERSION=$(git rev-parse --short "$GITHUB_SHA")
        docker build . --file ./src/consumer/Dockerfile --tag $REGISTRY/consumer:$VERSION
        docker push $REGISTRY/consumer:$VERSION
      
